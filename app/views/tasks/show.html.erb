<div class="top-header">
  <h3 class="text-center"><%= link_to @task.list.project.name, project_path(@task.list.project), class: 'secondary' %> | <%= link_to @task.list.name, list_path(@task.list), class: 'secondary' %> | <%= @task.name %></h3>
</div>
<div class="container">
  <div class="backed nomin">
    <%= render "shared/task", task: @task, index: 0 %>
  </div>
</div>
