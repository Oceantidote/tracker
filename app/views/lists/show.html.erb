<div class="top-header">
  <h3 class="text-center"><%= link_to @list.project.name, project_path(@list.project), class: 'secondary' %> | <%= link_to "To-dos", project_lists_path(@list.project), class: 'secondary' %> | <%= @list.name %></h3>
</div>
<div class="container">
  <div class="backed nomin">
    <%= render "shared/lists", lists: [@list] %>
  </div>
</div>
